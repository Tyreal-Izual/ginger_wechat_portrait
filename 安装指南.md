# 微信聊天记录分析工具 · 安装指南（Codex Fork）

> 本 fork 延续了上游项目的数据库解密方案，因此首次使用时仍然需要手动提取微信密钥。  
> 这一步和 Claude / Codex 无关，核心原因是 macOS 对 `lldb` 调试权限的限制。

---

## 自动完成的部分

在你已经拿到解密数据库、并导出了联系人聊天 CSV 之后，仓库可以自动完成：

- 加载聊天记录
- 生成词频、活跃时段、长度分布等统计图表
- 生成人格分析输入 JSON
- 由 Codex 按 schema 写出人格分析结果 JSON
- 生成完整 HTML 报告

---

## 需要你手动完成的部分

### 第 1 步：关闭 SIP

SIP（System Integrity Protection）会阻止调试器附加到微信进程。首次提取微信密钥前，需要临时关闭一次。

1. 完全关机
2. 进入恢复模式
3. 打开顶部菜单栏中的“终端”
4. 执行：

```bash
csrutil disable
reboot
```

5. 重启后验证：

```bash
csrutil status
```

如果输出里包含 `disabled`，说明关闭成功。

---

### 第 2 步：在系统 Terminal.app 中提取密钥

这一步必须在系统自带的 `Terminal.app` 里完成，不能依赖 Codex 内部终端。

推荐优先使用内存扫描版脚本。相比单次断点版，它更容易一次性扫出 `contact`、`message`、`session` 等多个数据库的密钥。

```bash
cd ~/Documents/wechat-db-decrypt-macos
PYTHONPATH="/Library/Developer/CommandLineTools/Library/PrivateFrameworks/LLDB.framework/Versions/A/Resources/Python" \
/Library/Developer/CommandLineTools/usr/bin/python3.9 find_key_memscan.py
```

执行后：

1. 脚本会附加到微信进程并扫描内存，期间可能持续几十秒到几分钟
2. 当终端出现 `[*] Detached from WeChat.` 时，表示扫描已经完成并从微信进程分离，这本身不是报错
3. 继续看最后几行结果；如果出现 `Results: X/Y keys found` 和 `[*] Keys saved to wechat_keys.json`，说明密钥文件已经成功写出
4. 如果结果里已经包含 `contact/contact.db`、`message/message_0.db`、`session/session.db` 这类关键库，就可以继续后面的解密和导出流程
5. 即使像 `migrate/unspportmsg.db` 这种非关键库没有找到密钥，通常也不影响聊天导出和画像报告生成

---

## Codex 推荐设置

为了让 Codex 在人格分析阶段保留尽可能多的上下文，建议配置为：

```toml
model_context_window = 1000000
model_auto_compact_token_limit = 900000
```

## 运行内存建议

- 建议最低 16GB 运行内存，推荐 24GB 及以上。
- 如果机器只有 8GB，请尽量关闭无关进程后再运行。
- 在 8GB 机器上执行大规模聊天导出、图表生成或长上下文分析时，系统压力可能过高，导致流程中断。

---

## 常见问题

### `ModuleNotFoundError: No module named 'lldb'`

请确认你用的是系统的 Python 3.9：

```bash
/Library/Developer/CommandLineTools/usr/bin/python3.9 --version
```

### Apple Silicon 架构错误

可以在命令前加：

```bash
arch -arm64
```

### `Permission denied` 或 `process attach denied`

说明 SIP 还没有成功关闭，请回到上一步重新确认。

---

## 解密完成后建议立即重新开启 SIP

拿到 `wechat_keys.json` 后，后续解密一般不再需要持续关闭 SIP，建议恢复系统保护：

```bash
csrutil enable
reboot
```

---

## 后续工作流

密钥与数据库就绪后，Codex 版工作流建议按下面顺序执行：

1. `python export_contact.py --contact "联系人名"`
2. `python codex_workflow.py prepare ./export_xxx.csv`
3. 让 Codex 读取 `wechat_analysis_output/codex_analysis_prompt.md` 并写出结果 JSON
4. `python codex_workflow.py finalize ./export_xxx.csv --self-result ...`

详细命令见 [README.md](./README.md)。
